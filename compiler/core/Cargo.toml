[package]
name = "slang_core"
version = "0.1.0"
edition = "2021"

[build-dependencies]
lalrpop = "0.19.7"

[dependencies]
lalrpop-util = { version = "0.19.7",  features = ["lexer"] }
regex = "1"
serde = { version = "1.0.151", features = ["serde_derive", "derive"] }
